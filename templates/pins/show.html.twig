{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} {{ pin.title }} Pin{% endblock %}

{% block body %}

<h1>{{ pin.title }}</h1>

<p>{{ pin.description }}</p>

<a href="{{ path('app_home')}}">Go back</a>
{% endblock %}
